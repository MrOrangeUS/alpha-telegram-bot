from telegram import schedule_rotating_content

schedule_rotating_content(TELEGRAM_CHAT_ID, TELEGRAM_BOT_TOKEN)

if __name__ == '__main__':
    port = int(os.environ.get('PORT', 5000))
    app.run(host='0.0.0.0', port=port)
